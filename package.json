{
  "name": "livy-client",
  "version": "0.0.1",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "node dist/livy-client.js",
    "build": "mkdir dist 1>/dev/null 2>&1; babel main.js -o dist/livy-client.js",
    "watch": "onchange main.js -- npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yuhsak Inoue",
  "license": "MIT",
  "devDependencies": {},
  "dependencies": {
    "axios": "^0.17.1"
  }
}
